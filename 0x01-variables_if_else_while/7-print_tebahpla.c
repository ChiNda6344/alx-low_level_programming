#include <stdio.h>
/**
 * main - program that prints lowercase alphabets in reverse
 * Return: 0 (success)
 */

int main(void)
{
	char b;

	b = 'a';
	while
		(b >= 'a') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
