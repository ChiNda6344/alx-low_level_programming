#include "main.h"
/**
 * mul - function checks for 0 through digits
 * @a: a - variable
 * @b: b - variable
 * Return: 0 (success)
 */

int mul(int a, int b)
{
	int mul:

	mul = a * b;
	return (mul);
}
