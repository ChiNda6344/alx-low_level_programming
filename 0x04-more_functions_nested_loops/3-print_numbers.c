#include "main.h"

/**
 * print_numbers - function checks for checks for a digit
 * Return: 0 (success)
 */

void print_numbers(void)
{
	int b;

	for (b = 48; b < 58; b++)
	{
		_putchar(b);
	}
		_putchar('\n');
}
